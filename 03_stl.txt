re (hour)
  examples
  DOTALL
  group
  (?P<name>...)
  groupdict
  sub
  EX: xml tag
  compile
  finditer
  prefer many short regexes over one big regex (perf vs. readabillity)

logging
  _levelNames
  getLogger
  handlers
  formatters
  filters
  tree and propagate

